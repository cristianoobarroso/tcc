
@{
    ViewBag.Title = "CadastroUsuario";
    Layout = "~/SharedMaster/MasterADM.cshtml";
}

@model Site.Models.ModelFuncionario


<title>Cadastro de Usuario</title>
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
<div class="row">
    <div class="col-sm-3">
    </div>
    <div class="col-sm-6">
        <div class="d1">
            <div id="DivIncluir" class="panel panel-primary">
                <div class="panel-heading">
                    <div class="panel-title text-center">Inclusão de Usuário</div>
                </div>
                <br />
                @using (Html.BeginForm("CadastroUsuario", "ADM", FormMethod.Post))
                {

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon">Nome Funcionario</span>
                        @Html.TextBoxFor(f => f.NomeFuncionario, new { @class = "col-xs10", @placeholder = "Digite aqui", style = "height:28;px;width:310px;", autocomplete = "off" })
                        @Html.ValidationMessageFor(u => u.NomeFuncionario)
                    </div>
                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon">Login</span>
                        @Html.TextBoxFor(f => f.Login, new { @class = "col-xs-10", @placeholder = "Digite aqui", style = "height:28px;width:140px;", autocomplete = "off" })
                        @Html.ValidationMessageFor(f => f.Login)
                        @*<input id="txtsobrenome" type="text"   />*@
                    </div>

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon">Senha</span>
                        @Html.TextBoxFor(f => f.Senha, new { @class = "col-xs-10", @placeholder = "Digite aqui", style = "height:28px;width:140px;", autocomplete = "off" })
                        @Html.ValidationMessageFor(f => f.Senha)
                        @*<input id="txtsobrenome" type="text"   />*@
                    </div>

                     List<SelectListItem> listPerfis = new List<SelectListItem>(); 
                        listPerfis.Add(new SelectListItem
                        {
                            Text = "Garçom",
                            Value = "1",
                            // Selected =
                        });
                        listPerfis.Add(new SelectListItem
                        {
                            Text = "Gerente",
                            Value = "2",
                            //Selected =
                        });

                       
                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon">Perfil</span>
                        @Html.DropDownList("ddlPerfil", (IEnumerable<SelectListItem>)listPerfis, "selecione", new { style = "height:28px;" })
                        @Html.ValidationMessageFor(f => f.Perfil)
                        @*<input id="txtsobrenome" type="text"   />*@
                    </div>

                    <div class="text-center">
                        <input type="submit" value="Incluir Usuario" class="btn btn-primary" />
                        <input action="action" type="button" value="Voltar" onclick="history.go(-1);" class="btn btn-primary" />
                    </div>
                    <br />
                    <div class="text-center">

                        <label class="FonteMenu">@ViewBag.Mensagem</label>
                    </div>
                }
            </div>
        </div>

        <div class="col-sm-3">
        </div>
    </div>
</div>

